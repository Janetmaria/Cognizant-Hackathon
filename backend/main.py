"""
Module 7 - FastAPI app entrypoint: forecast-serving + business logic.

Wires up routers from data_routes.py and reorder_routes.py, and exposes
/forecast/{store_id} directly. See docs/api_contract.md for the response
envelope and endpoint contracts (Module 8 owns that doc).
"""

# TODO(Module 7): create FastAPI app, include routers, define /forecast/{store_id}.
